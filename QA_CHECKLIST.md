# day-manager-app 最低限QAチェックリスト

## 1. 初期表示
- ページを再読み込みしても起動エラーが出ない
- `selectedDate` が今日になっている
- 左サイドバーの「今日」が active になっている
- Service Worker の影響で古い UI が出ていない

## 2. カレンダー導線
- カレンダー上の予定をクリックすると **右の詳細欄だけ** が更新される
- 予定クリックだけで編集フォームが勝手に開かない
- 詳細欄の「編集パネルで開く」を押すと、該当フォームに値が入る
- Google予定をクリックしたときは詳細欄から削除できる

## 3. 対象日連動
- 日付変更で `今日の判断` / `実行` / `Google予定` / `カレンダー` が同時に更新される
- `前日 / 今日 / 翌日` ボタンでズレずに再描画される

## 4. 予定・タスク編集
- 単発予定の追加後、一覧・概況・カレンダーに即反映される
- タスク状態変更（未着手→進行中→完了）で一覧・概況・今すぐ触る候補が即更新される
- 削除後にトーストの「元に戻す」が効く

## 5. 補助パネル
- 左の「学習 / 管理 / 連携」で対応する details が開く
- details の開閉が連打で壊れない
- quick add drawer を開閉してもスクロールが暴れない

## 6. デプロイ前
- `public/` に差し替えた内容がローカルで反映される
- Worker 配信構成なら `cloudflare-worker` で `npm run deploy` を実行する
- デプロイ後、ブラウザでハードリロードして再確認する
