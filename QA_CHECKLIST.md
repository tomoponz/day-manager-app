# Day Manager QA checklist (Phase 2)

## 差し替え後に最初にやること
1. `public/` 配下を上書きする
2. ブラウザで **ハードリロード** する
3. 以前の表示が残る場合は DevTools > Application > Service Workers / Cache Storage を確認する
4. Worker 配信で使っているなら `cloudflare-worker` で `npm run deploy`

## 重点確認
### 1. カレンダー導線
- 予定クリックで **詳細だけ** が右の詳細欄に出る
- 予定クリックだけでフォームが勝手に開かない
- 詳細欄の「編集パネルで開く」で右ドロワーが開く
- ローカル単発予定のドラッグ移動後に detail が更新される

### 2. 右ドロワー
- `管理・設定` 内の「固定予定を開く / 単発予定を開く / タスクを開く」で右ドロワーが開く
- 右ドロワー上部のタブで対象を切り替えられる
- `Esc` と backdrop クリックと「閉じる」で閉じる
- 保存後にドロワーが閉じる
- 編集取消でもドロワーが閉じる
- 閉じたあと、直前に押したボタンへフォーカスが戻る

### 3. 一覧からの編集
- 固定予定一覧の「編集」で右ドロワーが開く
- 単発予定一覧の「編集」で右ドロワーが開く
- タスク一覧の「編集」で右ドロワーが開く

### 4. 概況カード
- 「対象日の予定」「残り空き時間（対象日）」が対象日に応じて変わる
- 「48時間以内の締切（全体）」「未完了タスク（全体）」は対象日を変えても意味がぶれない
- 未完了タスクの並びが、タスク一覧と大きく矛盾しない

### 5. キャッシュ事故の再発防止
- 修正した JS/CSS/HTML がブラウザ上で本当に反映されているか確認
- GitHub に push しただけで Worker 公開が変わらない構成なら、deploy を別で実施


### 6. 自習場所管理
- `管理・設定` の「自習場所」で、場所名・営業時間を保存できる
- 保存後に `自習場所一覧` に表示される
- 一覧の「編集」でフォームに内容が戻る
- 一覧の「削除」で消え、トーストの「元に戻す」で復元できる
- 対象日を変えると `今日使える自習場所` の内容が変わる
- `AI連携（ChatGPT）` の文章に `今日使える自習場所` セクションが入る

### 7. キャッシュ更新
- 今回の差し替え後に Service Worker が新しいキャッシュ名へ切り替わる
- 古い表示が残る場合は `Ctrl + Shift + R` 後、必要なら Service Worker / Cache Storage を削除する
